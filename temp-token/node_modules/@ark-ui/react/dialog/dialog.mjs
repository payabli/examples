'use client';
import { jsx } from 'react/jsx-runtime';
import 'react';
import { runIfFn } from '../run-if-fn.mjs';
import { DialogProvider } from './dialog-context.mjs';
import { useDialog } from './use-dialog.mjs';

const Dialog = (props) => {
  const { children, ...useDialogProps } = props;
  const dialog = useDialog(useDialogProps);
  const view = runIfFn(children, dialog);
  return /* @__PURE__ */ jsx(DialogProvider, { value: dialog, children: view });
};

export { Dialog };
