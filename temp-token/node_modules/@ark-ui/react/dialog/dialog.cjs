'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
require('react');
const runIfFn = require('../run-if-fn.cjs');
const dialogContext = require('./dialog-context.cjs');
const useDialog = require('./use-dialog.cjs');

const Dialog = (props) => {
  const { children, ...useDialogProps } = props;
  const dialog = useDialog.useDialog(useDialogProps);
  const view = runIfFn.runIfFn(children, dialog);
  return /* @__PURE__ */ jsxRuntime.jsx(dialogContext.DialogProvider, { value: dialog, children: view });
};

exports.Dialog = Dialog;
