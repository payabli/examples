'use client';
import { jsx } from 'react/jsx-runtime';
import 'react';
import { runIfFn } from '../run-if-fn.mjs';
import { TooltipProvider } from './tooltip-context.mjs';
import { useTooltip } from './use-tooltip.mjs';

const Tooltip = (props) => {
  const { children, ...useTooltipProps } = props;
  const tooltip = useTooltip(useTooltipProps);
  const view = runIfFn(children, tooltip);
  return /* @__PURE__ */ jsx(TooltipProvider, { value: tooltip, children: view });
};

export { Tooltip };
