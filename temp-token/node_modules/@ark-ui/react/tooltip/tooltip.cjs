'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
require('react');
const runIfFn = require('../run-if-fn.cjs');
const tooltipContext = require('./tooltip-context.cjs');
const useTooltip = require('./use-tooltip.cjs');

const Tooltip = (props) => {
  const { children, ...useTooltipProps } = props;
  const tooltip = useTooltip.useTooltip(useTooltipProps);
  const view = runIfFn.runIfFn(children, tooltip);
  return /* @__PURE__ */ jsxRuntime.jsx(tooltipContext.TooltipProvider, { value: tooltip, children: view });
};

exports.Tooltip = Tooltip;
