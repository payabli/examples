'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { TabsProvider } from './tabs-context.mjs';
import { useTabs } from './use-tabs.mjs';

const Tabs = forwardRef((props, ref) => {
  const [useTabsProps, divProps] = createSplitProps()(props, [
    "activationMode",
    "defaultValue",
    "dir",
    "getRootNode",
    "id",
    "ids",
    "loop",
    "onChange",
    "onFocus",
    "orientation",
    "translations",
    "value"
  ]);
  const tabs = useTabs(useTabsProps);
  const mergedProps = mergeProps(tabs.rootProps, divProps);
  return /* @__PURE__ */ jsx(TabsProvider, { value: tabs, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref }) });
});
Tabs.displayName = "Tabs";

export { Tabs };
