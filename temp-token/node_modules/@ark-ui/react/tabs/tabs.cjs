'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react$1 = require('@zag-js/react');
const react = require('react');
const createSplitProps = require('../create-split-props.cjs');
const factory = require('../factory.cjs');
const tabsContext = require('./tabs-context.cjs');
const useTabs = require('./use-tabs.cjs');

const Tabs = react.forwardRef((props, ref) => {
  const [useTabsProps, divProps] = createSplitProps.createSplitProps()(props, [
    "activationMode",
    "defaultValue",
    "dir",
    "getRootNode",
    "id",
    "ids",
    "loop",
    "onChange",
    "onFocus",
    "orientation",
    "translations",
    "value"
  ]);
  const tabs = useTabs.useTabs(useTabsProps);
  const mergedProps = react$1.mergeProps(tabs.rootProps, divProps);
  return /* @__PURE__ */ jsxRuntime.jsx(tabsContext.TabsProvider, { value: tabs, children: /* @__PURE__ */ jsxRuntime.jsx(factory.ark.div, { ...mergedProps, ref }) });
});
Tabs.displayName = "Tabs";

exports.Tabs = Tabs;
