'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { ark } from '../factory.mjs';
import { useRangeSliderContext } from './range-slider-context.mjs';

const RangeSliderTrack = forwardRef((props, ref) => {
  const { trackProps } = useRangeSliderContext();
  const mergedProps = mergeProps(trackProps, props);
  return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref });
});
RangeSliderTrack.displayName = "RangeSliderTrack";

export { RangeSliderTrack };
