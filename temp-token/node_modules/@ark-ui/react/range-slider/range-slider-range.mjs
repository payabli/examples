'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { ark } from '../factory.mjs';
import { useRangeSliderContext } from './range-slider-context.mjs';

const RangeSliderRange = forwardRef((props, ref) => {
  const { rangeProps } = useRangeSliderContext();
  const mergedProps = mergeProps(rangeProps, props);
  return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref });
});
RangeSliderRange.displayName = "RangeSliderRange";

export { RangeSliderRange };
