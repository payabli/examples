'use client';
import { jsx } from 'react/jsx-runtime';
import '@zag-js/menu';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { runIfFn } from '../run-if-fn.mjs';
import { useMenuContext } from './menu-context.mjs';

const MenuOptionItem = forwardRef((props, ref) => {
  const api = useMenuContext();
  const [optionProps, { children, ...divProps }] = createSplitProps()(props, [
    "id",
    "disabled",
    "valueText",
    "closeOnSelect",
    "name",
    "type",
    "value",
    "onCheckedChange"
  ]);
  const view = runIfFn(children, { isActive: api?.isOptionChecked(optionProps) ?? false });
  const mergedProps = mergeProps(api?.getOptionItemProps(optionProps) ?? {}, divProps);
  return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref, children: view });
});
MenuOptionItem.displayName = "MenuOptionItem";

export { MenuOptionItem };
