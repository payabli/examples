'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { AccordionProvider } from './accordion-context.mjs';
import { useAccordion } from './use-accordion.mjs';

const Accordion = forwardRef((props, ref) => {
  const [useAccordionProps, divProps] = createSplitProps()(props, [
    "collapsible",
    "defaultValue",
    "dir",
    "disabled",
    "getRootNode",
    "id",
    "ids",
    "multiple",
    "onChange",
    "onFocusChange",
    "orientation",
    "value"
  ]);
  const accordion = useAccordion(useAccordionProps);
  const mergedProps = mergeProps(accordion.rootProps, divProps);
  return /* @__PURE__ */ jsx(AccordionProvider, { value: accordion, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref }) });
});
Accordion.displayName = "Accordion";

export { Accordion };
