'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { ColorPickerChannelSliderProvider } from './color-picker-channel-slider-context.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';

const ColorPickerChannelSliderTrack = forwardRef((props, ref) => {
  const [channelProps, localProps] = createSplitProps()(props, [
    "channel",
    "orientation"
  ]);
  const { getChannelSliderTrackProps } = useColorPickerContext();
  const mergedProps = mergeProps(getChannelSliderTrackProps(channelProps), localProps);
  return /* @__PURE__ */ jsx(ColorPickerChannelSliderProvider, { value: channelProps, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref }) });
});
ColorPickerChannelSliderTrack.displayName = "ColorPickerChannelSliderTrack";

export { ColorPickerChannelSliderTrack };
