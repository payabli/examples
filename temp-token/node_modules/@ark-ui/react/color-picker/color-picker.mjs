'use client';
import { jsx } from 'react/jsx-runtime';
import 'react';
import { runIfFn } from '../run-if-fn.mjs';
import { ColorPickerProvider } from './color-picker-context.mjs';
import { useColorPicker } from './use-color-picker.mjs';

const ColorPicker = (props) => {
  const { children, ...useColorPickerProps } = props;
  const colorPicker = useColorPicker(useColorPickerProps);
  const view = runIfFn(children, colorPicker);
  return /* @__PURE__ */ jsx(ColorPickerProvider, { value: colorPicker, children: view });
};

export { ColorPicker };
