'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
require('react');
const runIfFn = require('../run-if-fn.cjs');
const colorPickerContext = require('./color-picker-context.cjs');
const useColorPicker = require('./use-color-picker.cjs');

const ColorPicker = (props) => {
  const { children, ...useColorPickerProps } = props;
  const colorPicker = useColorPicker.useColorPicker(useColorPickerProps);
  const view = runIfFn.runIfFn(children, colorPicker);
  return /* @__PURE__ */ jsxRuntime.jsx(colorPickerContext.ColorPickerProvider, { value: colorPicker, children: view });
};

exports.ColorPicker = ColorPicker;
