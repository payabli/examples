'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { useColorPickerContext } from './color-picker-context.mjs';
import { ColorPickerSwatchProvider } from './color-picker-swatch-context.mjs';

const ColorPickerSwatch = forwardRef(
  (props, ref) => {
    const [colorSwatchProps, localProps] = createSplitProps()(props, [
      "readOnly",
      "value"
    ]);
    const { getSwatchProps } = useColorPickerContext();
    const mergedProps = mergeProps(getSwatchProps(colorSwatchProps), localProps);
    return /* @__PURE__ */ jsx(ColorPickerSwatchProvider, { value: colorSwatchProps, children: /* @__PURE__ */ jsx(ark.button, { disabled: colorSwatchProps.readOnly, ...mergedProps, ref }) });
  }
);
ColorPickerSwatch.displayName = "ColorPickerSwatch";

export { ColorPickerSwatch };
