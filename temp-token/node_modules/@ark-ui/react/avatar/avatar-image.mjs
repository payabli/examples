'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { ark } from '../factory.mjs';
import { useAvatarContext } from './avatar-context.mjs';

const AvatarImage = forwardRef((props, ref) => {
  const { imageProps } = useAvatarContext();
  const mergedProps = mergeProps(imageProps, props);
  return /* @__PURE__ */ jsx(ark.img, { ...mergedProps, ref });
});
AvatarImage.displayName = "AvatarImage";

export { AvatarImage };
