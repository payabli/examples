'use client';
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { ark } from '../factory.mjs';
import { parts } from './date-picker.anatomy.mjs';

const DatePickerRowGroup = forwardRef(
  (props, ref) => /* @__PURE__ */ jsx(ark.div, { role: "rowgroup", ...parts.rowGroup.attrs, ...props, ref })
);
DatePickerRowGroup.displayName = "DatePickerRowGroup";

export { DatePickerRowGroup };
