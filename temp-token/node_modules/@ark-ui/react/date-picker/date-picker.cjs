'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
require('react');
const runIfFn = require('../run-if-fn.cjs');
const datePickerContext = require('./date-picker-context.cjs');
const useDatePicker = require('./use-date-picker.cjs');

const DatePicker = (props) => {
  const { children, ...useDatePickerProps } = props;
  const datePicker = useDatePicker.useDatePicker(useDatePickerProps);
  const view = runIfFn.runIfFn(children, datePicker);
  return /* @__PURE__ */ jsxRuntime.jsx(datePickerContext.DatePickerProvider, { value: datePicker, children: view });
};

exports.DatePicker = DatePicker;
