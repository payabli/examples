'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';

const DatePickerRowHeader = forwardRef(
  (props, ref) => {
    const { getHeaderProps } = useDatePickerContext();
    const mergedProps = mergeProps(getHeaderProps({ view: "day" }), props);
    return /* @__PURE__ */ jsx(ark.div, { role: "row", ...mergedProps, "aria-hidden": false, ref });
  }
);
DatePickerRowHeader.displayName = "DatePickerRowHeader";

export { DatePickerRowHeader };
