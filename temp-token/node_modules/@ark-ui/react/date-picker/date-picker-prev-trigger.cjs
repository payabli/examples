'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react$1 = require('@zag-js/react');
const react = require('react');
const factory = require('../factory.cjs');
const datePickerContext = require('./date-picker-context.cjs');

const DatePickerPrevTrigger = react.forwardRef(
  (props, ref) => {
    const { getPrevTriggerProps, view } = datePickerContext.useDatePickerContext();
    const mergedProps = react$1.mergeProps(getPrevTriggerProps({ view }), props);
    return /* @__PURE__ */ jsxRuntime.jsx(factory.ark.button, { ...mergedProps, ref });
  }
);
DatePickerPrevTrigger.displayName = "DatePickerPrevTrigger";

exports.DatePickerPrevTrigger = DatePickerPrevTrigger;
