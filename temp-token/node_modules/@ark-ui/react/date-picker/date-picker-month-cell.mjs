'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { DatePickerCellProvider } from './date-picker-cell-context.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';

const DatePickerMonthCell = forwardRef(
  (props, ref) => {
    const { getMonthCellProps } = useDatePickerContext();
    const [cellProps, localProps] = createSplitProps()(props, ["value", "disabled"]);
    const mergedProps = mergeProps(getMonthCellProps(cellProps), localProps);
    return /* @__PURE__ */ jsx(DatePickerCellProvider, { value: cellProps, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref }) });
  }
);
DatePickerMonthCell.displayName = "DatePickerMonthCell";

export { DatePickerMonthCell };
