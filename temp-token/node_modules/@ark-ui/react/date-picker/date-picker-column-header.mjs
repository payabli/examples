'use client';
import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { ark } from '../factory.mjs';
import { parts } from './date-picker.anatomy.mjs';

const DatePickerColumnHeader = forwardRef(
  (props, ref) => {
    return /* @__PURE__ */ jsx(ark.div, { role: "columnheader", ...parts.columnHeader.attrs, ...props, ref });
  }
);
DatePickerColumnHeader.displayName = "DatePickerColumnHeader";

export { DatePickerColumnHeader };
