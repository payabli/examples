'use client';
import { jsx } from 'react/jsx-runtime';
import 'react';
import { runIfFn } from '../run-if-fn.mjs';
import { DatePickerProvider } from './date-picker-context.mjs';
import { useDatePicker } from './use-date-picker.mjs';

const DatePicker = (props) => {
  const { children, ...useDatePickerProps } = props;
  const datePicker = useDatePicker(useDatePickerProps);
  const view = runIfFn(children, datePicker);
  return /* @__PURE__ */ jsx(DatePickerProvider, { value: datePicker, children: view });
};

export { DatePicker };
