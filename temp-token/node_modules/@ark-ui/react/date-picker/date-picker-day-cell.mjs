'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';
import { DatePickerDayCellProvider } from './date-picker-day-cell-context.mjs';

const DatePickerDayCell = forwardRef(
  (props, ref) => {
    const { getDayCellProps } = useDatePickerContext();
    const [cellProps, localProps] = createSplitProps()(props, [
      "value",
      "disabled",
      "offset"
    ]);
    const mergedProps = mergeProps(getDayCellProps(cellProps), localProps);
    return /* @__PURE__ */ jsx(DatePickerDayCellProvider, { value: cellProps, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref }) });
  }
);
DatePickerDayCell.displayName = "DatePickerDayCell";

export { DatePickerDayCell };
