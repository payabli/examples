'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { ark } from '../factory.mjs';
import { useDatePickerContext } from './date-picker-context.mjs';

const DatePickerMonthSelect = forwardRef(
  (props, ref) => {
    const { monthSelectProps, getMonths } = useDatePickerContext();
    const mergedProps = mergeProps(monthSelectProps, props);
    return /* @__PURE__ */ jsx(ark.select, { ...mergedProps, ref, children: getMonths().map((month, i) => /* @__PURE__ */ jsx("option", { value: month.value, children: month.label }, i)) });
  }
);
DatePickerMonthSelect.displayName = "DatePickerMonthSelect";

export { DatePickerMonthSelect };
