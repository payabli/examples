'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react$1 = require('@zag-js/react');
const react = require('react');
const factory = require('../factory.cjs');
const runIfFn = require('../run-if-fn.cjs');
const sliderContext = require('./slider-context.cjs');

const SliderOutput = react.forwardRef((props, ref) => {
  const { children, ...restProps } = props;
  const slider = sliderContext.useSliderContext();
  const mergedProps = react$1.mergeProps(slider.outputProps, restProps);
  const view = runIfFn.runIfFn(children, slider);
  return /* @__PURE__ */ jsxRuntime.jsx(factory.ark.output, { ...mergedProps, ref, children: view ?? slider.value });
});
SliderOutput.displayName = "SliderOutput";

exports.SliderOutput = SliderOutput;
