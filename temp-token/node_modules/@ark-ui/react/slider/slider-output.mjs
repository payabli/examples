'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { ark } from '../factory.mjs';
import { runIfFn } from '../run-if-fn.mjs';
import { useSliderContext } from './slider-context.mjs';

const SliderOutput = forwardRef((props, ref) => {
  const { children, ...restProps } = props;
  const slider = useSliderContext();
  const mergedProps = mergeProps(slider.outputProps, restProps);
  const view = runIfFn(children, slider);
  return /* @__PURE__ */ jsx(ark.output, { ...mergedProps, ref, children: view ?? slider.value });
});
SliderOutput.displayName = "SliderOutput";

export { SliderOutput };
