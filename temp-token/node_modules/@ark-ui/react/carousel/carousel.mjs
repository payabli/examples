'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { runIfFn } from '../run-if-fn.mjs';
import { CarouselProvider } from './carousel-context.mjs';
import { useCarousel } from './use-carousel.mjs';

const Carousel = forwardRef((props, ref) => {
  const [useCarouselProps, { children, ...divProps }] = createSplitProps()(
    props,
    [
      "align",
      "dir",
      "getRootNode",
      "id",
      "ids",
      "index",
      "loop",
      "onSlideChange",
      "orientation",
      "slidesPerView",
      "spacing"
    ]
  );
  const carousel = useCarousel(useCarouselProps);
  const mergedProps = mergeProps(carousel.rootProps, divProps);
  const view = runIfFn(children, carousel);
  return /* @__PURE__ */ jsx(CarouselProvider, { value: carousel, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref, children: view }) });
});
Carousel.displayName = "Carousel";

export { Carousel };
