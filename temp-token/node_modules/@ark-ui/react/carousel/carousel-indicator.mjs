'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { useCarouselContext } from './carousel-context.mjs';

const CarouselIndicator = forwardRef(
  (props, ref) => {
    const [indicatorProps, buttonProps] = createSplitProps()(props, [
      "readOnly",
      "index"
    ]);
    const { getIndicatorProps } = useCarouselContext();
    const mergedProps = mergeProps(getIndicatorProps(indicatorProps), buttonProps);
    return /* @__PURE__ */ jsx(ark.button, { ...mergedProps, ref });
  }
);
CarouselIndicator.displayName = "CarouselIndicator";

export { CarouselIndicator };
