'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react$1 = require('@zag-js/react');
const react = require('react');
const createSplitProps = require('../create-split-props.cjs');
const factory = require('../factory.cjs');
const runIfFn = require('../run-if-fn.cjs');
const carouselContext = require('./carousel-context.cjs');
const useCarousel = require('./use-carousel.cjs');

const Carousel = react.forwardRef((props, ref) => {
  const [useCarouselProps, { children, ...divProps }] = createSplitProps.createSplitProps()(
    props,
    [
      "align",
      "dir",
      "getRootNode",
      "id",
      "ids",
      "index",
      "loop",
      "onSlideChange",
      "orientation",
      "slidesPerView",
      "spacing"
    ]
  );
  const carousel = useCarousel.useCarousel(useCarouselProps);
  const mergedProps = react$1.mergeProps(carousel.rootProps, divProps);
  const view = runIfFn.runIfFn(children, carousel);
  return /* @__PURE__ */ jsxRuntime.jsx(carouselContext.CarouselProvider, { value: carousel, children: /* @__PURE__ */ jsxRuntime.jsx(factory.ark.div, { ...mergedProps, ref, children: view }) });
});
Carousel.displayName = "Carousel";

exports.Carousel = Carousel;
