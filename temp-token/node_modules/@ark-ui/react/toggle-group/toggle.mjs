'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { useToggleGroupContext } from './toggle-group-context.mjs';

const Toggle = forwardRef((props, ref) => {
  const [toggleProps, localProps] = createSplitProps()(props, ["value", "disabled"]);
  const api = useToggleGroupContext();
  const mergedProps = mergeProps(api.getToggleProps(toggleProps), localProps);
  return /* @__PURE__ */ jsx(ark.button, { ...mergedProps, ref });
});
Toggle.displayName = "Toggle";

export { Toggle };
