'use client';
import { jsx } from 'react/jsx-runtime';
import 'react';
import { runIfFn } from '../run-if-fn.mjs';
import { HoverCardProvider } from './hover-card-context.mjs';
import { useHoverCard } from './use-hover-card.mjs';

const HoverCard = (props) => {
  const { children, ...useHoverCardProps } = props;
  const hoverCard = useHoverCard(useHoverCardProps);
  const view = runIfFn(children, hoverCard);
  return /* @__PURE__ */ jsx(HoverCardProvider, { value: hoverCard, children: view });
};

export { HoverCard };
