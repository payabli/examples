'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
require('react');
const runIfFn = require('../run-if-fn.cjs');
const hoverCardContext = require('./hover-card-context.cjs');
const useHoverCard = require('./use-hover-card.cjs');

const HoverCard = (props) => {
  const { children, ...useHoverCardProps } = props;
  const hoverCard = useHoverCard.useHoverCard(useHoverCardProps);
  const view = runIfFn.runIfFn(children, hoverCard);
  return /* @__PURE__ */ jsxRuntime.jsx(hoverCardContext.HoverCardProvider, { value: hoverCard, children: view });
};

exports.HoverCard = HoverCard;
