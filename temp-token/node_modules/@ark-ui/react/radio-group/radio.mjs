'use client';
import { jsxs, jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { RadioProvider } from './radio-context.mjs';
import { useRadioGroupContext } from './radio-group-context.mjs';

const Radio = forwardRef((props, ref) => {
  const [radioProps, localProps] = createSplitProps()(props, [
    "value",
    "disabled",
    "invalid"
  ]);
  const api = useRadioGroupContext();
  const mergedProps = mergeProps(api.getRadioProps(radioProps), localProps);
  return /* @__PURE__ */ jsxs(RadioProvider, { value: props, children: [
    /* @__PURE__ */ jsx(ark.label, { ...mergedProps, ref }),
    /* @__PURE__ */ jsx("input", { ...api.getRadioHiddenInputProps(radioProps) })
  ] });
});
Radio.displayName = "Radio";

export { Radio };
