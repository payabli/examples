'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react$1 = require('@zag-js/react');
const react = require('react');
const createSplitProps = require('../create-split-props.cjs');
const factory = require('../factory.cjs');
const runIfFn = require('../run-if-fn.cjs');
const paginationContext = require('./pagination-context.cjs');
const usePagination = require('./use-pagination.cjs');

const Pagination = react.forwardRef((props, ref) => {
  const [paginationProps, { children, ...navProps }] = createSplitProps.createSplitProps()(
    props,
    [
      "count",
      "defaultPage",
      "dir",
      "getRootNode",
      "id",
      "ids",
      "onChange",
      "page",
      "pageSize",
      "siblingCount",
      "translations",
      "type"
    ]
  );
  const pagination = usePagination.usePagination(paginationProps);
  const view = runIfFn.runIfFn(children, pagination);
  const mergedProps = react$1.mergeProps(pagination.rootProps, navProps);
  return /* @__PURE__ */ jsxRuntime.jsx(paginationContext.PaginationProvider, { value: pagination, children: /* @__PURE__ */ jsxRuntime.jsx(factory.ark.nav, { ...mergedProps, ref, children: view }) });
});
Pagination.displayName = "Pagination";

exports.Pagination = Pagination;
