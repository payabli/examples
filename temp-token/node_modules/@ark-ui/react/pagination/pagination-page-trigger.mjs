'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { forwardRef } from 'react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { usePaginationContext } from './pagination-context.mjs';

const PaginationPageTrigger = forwardRef(
  (props, ref) => {
    const [paginationProps, localProps] = createSplitProps()(props, [
      "type",
      "value"
    ]);
    const { getPageTriggerProps } = usePaginationContext();
    const mergedProps = mergeProps(getPageTriggerProps(paginationProps), localProps);
    return /* @__PURE__ */ jsx(ark.button, { ...mergedProps, ref });
  }
);
PaginationPageTrigger.displayName = "PaginationPageTrigger";

export { PaginationPageTrigger };
