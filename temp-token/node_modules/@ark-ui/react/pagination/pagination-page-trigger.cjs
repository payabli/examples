'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react$1 = require('@zag-js/react');
const react = require('react');
const createSplitProps = require('../create-split-props.cjs');
const factory = require('../factory.cjs');
const paginationContext = require('./pagination-context.cjs');

const PaginationPageTrigger = react.forwardRef(
  (props, ref) => {
    const [paginationProps, localProps] = createSplitProps.createSplitProps()(props, [
      "type",
      "value"
    ]);
    const { getPageTriggerProps } = paginationContext.usePaginationContext();
    const mergedProps = react$1.mergeProps(getPageTriggerProps(paginationProps), localProps);
    return /* @__PURE__ */ jsxRuntime.jsx(factory.ark.button, { ...mergedProps, ref });
  }
);
PaginationPageTrigger.displayName = "PaginationPageTrigger";

exports.PaginationPageTrigger = PaginationPageTrigger;
