'use client';
'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
require('react');
const runIfFn = require('../run-if-fn.cjs');
const popoverContext = require('./popover-context.cjs');
const usePopover = require('./use-popover.cjs');

const Popover = (props) => {
  const { children, ...usePopoverProps } = props;
  const popover = usePopover.usePopover(usePopoverProps);
  const view = runIfFn.runIfFn(children, popover);
  return /* @__PURE__ */ jsxRuntime.jsx(popoverContext.PopoverProvider, { value: popover, children: view });
};

exports.Popover = Popover;
