'use client';
import { jsx } from 'react/jsx-runtime';
import 'react';
import { runIfFn } from '../run-if-fn.mjs';
import { PopoverProvider } from './popover-context.mjs';
import { usePopover } from './use-popover.mjs';

const Popover = (props) => {
  const { children, ...usePopoverProps } = props;
  const popover = usePopover(usePopoverProps);
  const view = runIfFn(children, popover);
  return /* @__PURE__ */ jsx(PopoverProvider, { value: popover, children: view });
};

export { Popover };
