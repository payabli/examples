import { ChatSession, FormConfig } from './types';
interface FormBodyProps {
    fields: FormConfig['fields'];
    submitButtonLabel: FormConfig['submitButtonLabel'];
    submitCallback: FormConfig['submitCallback'];
    onClose: () => void;
    successCallback: () => void;
    chatSession?: ChatSession;
}
declare function FormBody({ fields, submitButtonLabel, submitCallback, onClose, successCallback, chatSession, }: FormBodyProps): import("react/jsx-runtime").JSX.Element;
export default FormBody;
