import { ChatSession, FormConfig } from './types';
interface FormStageManagerProps {
    formConfig: FormConfig;
    onClose: () => void;
    onModalClose?: () => void;
    isMdContainer?: boolean;
    chatSession?: ChatSession;
}
declare function FormStageManager({ formConfig, onClose, onModalClose, isMdContainer, chatSession, }: FormStageManagerProps): import("react/jsx-runtime").JSX.Element;
export default FormStageManager;
