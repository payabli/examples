import type { Control, FieldErrors, FieldValues, UseFormRegister } from 'react-hook-form';
import type { FormField as FormFieldType } from './types';
export interface FormFieldProps extends FormFieldType {
    errors: FieldErrors;
    autoFocus?: boolean;
    register: UseFormRegister<FieldValues>;
    control: Control;
}
declare function FormField({ name, label, inputType, placeholder, required, errors, register, control, autoFocus, isHidden, selectOptions, }: FormFieldProps): import("react/jsx-runtime").JSX.Element;
export default FormField;
