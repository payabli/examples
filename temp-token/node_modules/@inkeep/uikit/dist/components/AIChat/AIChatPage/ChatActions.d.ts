import type { ReactElement } from 'react';
import type { Feedback } from '../../../hooks/useMessageFeedback';
interface ChatActionsProps {
    ind: number;
    isHidden?: boolean;
    setShowFeedbackModal?: (ind: number) => void;
    submitPositiveFeedback?: (ind: number) => void;
    feedback: Feedback;
    handleCopyMessage: () => void;
    copyableMessage: string;
    supportEscalation?: ReactElement | null;
}
declare function ChatActions({ ind, isHidden, setShowFeedbackModal, submitPositiveFeedback, feedback, handleCopyMessage, copyableMessage, supportEscalation, }: ChatActionsProps): import("react/jsx-runtime").JSX.Element;
export default ChatActions;
