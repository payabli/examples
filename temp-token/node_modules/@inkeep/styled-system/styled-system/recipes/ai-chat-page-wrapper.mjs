import { splitProps } from '../helpers.mjs';
import { createRecipe, mergeRecipes } from './create-recipe.mjs';

const aiChatPageWrapperFn = /* @__PURE__ */ createRecipe('ai-chat-page-wrapper', {
  "size": "default",
  "variant": "container-with-shadow"
}, [])

const aiChatPageWrapperVariantMap = {
  "size": [
    "expand",
    "default",
    "shrink-vertically",
    "full-viewport"
  ],
  "variant": [
    "no-shadow",
    "container-with-shadow"
  ]
}

const aiChatPageWrapperVariantKeys = Object.keys(aiChatPageWrapperVariantMap)

export const aiChatPageWrapper = /* @__PURE__ */ Object.assign(aiChatPageWrapperFn, {
  __recipe__: true,
  __name__: 'aiChatPageWrapper',
  raw: (props) => props,
  variantKeys: aiChatPageWrapperVariantKeys,
  variantMap: aiChatPageWrapperVariantMap,
  merge(recipe) {
    return mergeRecipes(this, recipe)
  },
  splitVariantProps(props) {
    return splitProps(props, aiChatPageWrapperVariantKeys)
  },
})