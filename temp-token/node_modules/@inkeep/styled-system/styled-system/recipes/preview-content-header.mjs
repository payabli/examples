import { splitProps } from '../helpers.mjs';
import { createRecipe, mergeRecipes } from './create-recipe.mjs';

const previewContentHeaderFn = /* @__PURE__ */ createRecipe('preview-content-header', {}, [])

const previewContentHeaderVariantMap = {
  "variant": [
    "title",
    "hitContentPreview"
  ]
}

const previewContentHeaderVariantKeys = Object.keys(previewContentHeaderVariantMap)

export const previewContentHeader = /* @__PURE__ */ Object.assign(previewContentHeaderFn, {
  __recipe__: true,
  __name__: 'previewContentHeader',
  raw: (props) => props,
  variantKeys: previewContentHeaderVariantKeys,
  variantMap: previewContentHeaderVariantMap,
  merge(recipe) {
    return mergeRecipes(this, recipe)
  },
  splitVariantProps(props) {
    return splitProps(props, previewContentHeaderVariantKeys)
  },
})